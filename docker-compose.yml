version: "2"

services:
  sygal-import-ws:
    container_name: sygal-import-ws-container
    build:
      context: .
    ports:
     - "8080:80"
     - "8443:443"
    volumes:
     - .:/var/www/sygal-import-ws
    working_dir: /var/www/sygal-import-ws
    network_mode: bridge
